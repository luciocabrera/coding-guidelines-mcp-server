# Copilot Instructions

- Use the MCP resources in this repo for coding standards.
- If uncertain, call `search_guidelines` or `get_guideline_summary`.
- Validate code patterns with `validate_code_pattern` before suggesting changes.

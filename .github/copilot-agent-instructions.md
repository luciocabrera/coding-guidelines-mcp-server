## CRITICAL: Read ESLint Rules First

BEFORE generating ANY code, ALWAYS review:

- guidelines://eslint-rules

When generating code:

- Follow the repository guidelines resources.
- Prefer calling MCP tools (`search_guidelines`, `get_guideline_summary`, `validate_code_pattern`, `generate_code`) instead of guessing.
